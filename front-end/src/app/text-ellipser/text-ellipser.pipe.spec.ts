import { TextEllipserPipe } from './text-ellipser.pipe';

describe('TextEllipserPipe', () => {
  it('create an instance', () => {
    const pipe = new TextEllipserPipe();
    expect(pipe).toBeTruthy();
  });
});
